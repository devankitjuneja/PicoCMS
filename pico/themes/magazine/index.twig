<!DOCTYPE html>
<!--[if lt IE 7]>      <html class="no-js lt-ie9 lt-ie8 lt-ie7"> <![endif]-->
<!--[if IE 7]>         <html class="no-js lt-ie9 lt-ie8"> <![endif]-->
<!--[if IE 8]>         <html class="no-js lt-ie9"> <![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js"> <!--<![endif]-->
	<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<title>{{ site_title }} - {{ meta.description|striptags }}</title>
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<meta name="description" content="{{ meta.description|striptags }}" />
	<meta name="author" content="{{ config.author }}" />

  	<!-- Facebook and Twitter integration -->
	<meta property="og:title" content="{{ site_title }}"/>
	<meta property="og:image" content="{{ config.image }}"/>
	<meta property="og:url" content="{{ base_url }}"/>
	<meta property="og:site_name" content="{{ site_title }}"/>
	<meta property="og:description" content="{{ meta.description|striptags }}"/>
	<meta name="twitter:title" content="{{ site_title }}" />
	<meta name="twitter:image" content="{{ config.image }}" />
	<meta name="twitter:url" content="{{ base_url }}" />
	<meta name="twitter:card" content="summary_large_image" />

	<!-- Place favicon.ico and apple-touch-icon.png in the root directory -->
	<link rel="shortcut icon" href="{{ theme_url }}/favicon.ico">
	<!-- Google Fonts -->
	<link href='http://fonts.googleapis.com/css?family=Playfair+Display:400,700,400italic|Roboto:400,300,700' rel='stylesheet' type='text/css'>
	<!-- Animate -->
	<link rel="stylesheet" href="{{ theme_url }}/css/animate.css">
	<!-- Icomoon -->
	<link rel="stylesheet" href="{{ theme_url }}/css/icomoon.css">
	<!-- Bootstrap  -->
	<link rel="stylesheet" href="{{ theme_url }}/css/bootstrap.css">

	<link rel="stylesheet" href="{{ theme_url }}/css/style.css">
	<style>
		div.pager {text-align: center;margin: 1em 0;}
		div.pager span {display: inline-block;width: 1.8em;height: 1.8em;line-height: 1.8;text-align: center;cursor: pointer;background: #000;color: #fff;margin-right: 0.5em;}
		div.pager span.active {background: #F7CA78;}
	</style>
	<!-- Modernizr JS -->
	<script defer src="{{ theme_url }}/js/modernizr-2.6.2.min.js"></script>
	<!-- FOR IE9 below -->
	<!--[if lt IE 9]>
	<script src="{{ theme_url }}/js/respond.min.js"></script>
	<![endif]-->
	<script type="application/ld+json">
	{
		"@context": "http://schema.org",
		"@type": "WebSite",
		"name" : "{{ site_title }}",
		"url": "{{ base_url }}",
		"potentialAction": {
		  "@type": "SearchAction",
		  "target": "{{ 'search'|link }}?q={q}",
		  "query-input": "required name=q"
		}
	}
	</script>
	</head>
	<body>
	<div id="fh5co-offcanvas" itemscope="itemscope" itemtype="http://schema.org/WPSideBar">
		<a href="#" class="fh5co-close-offcanvas js-fh5co-close-offcanvas"><span><i class="icon-cross3"></i> <span>Close</span></span></a>
		<div class="fh5co-bio">
			<figure>
				{% if config.authorimage %}
				<img src="{{ config.authorimage }}" alt="{{ config.author }}" class="img-responsive">
				{% else %}
				<img src="{{ config.authorimagealt }}" alt="{{ config.author }}" class="img-responsive">
				{% endif %}
			</figure>
			<h3 class="heading">About Me</h3>
			<h2>{{ config.author }}</h2>
			<p>{{ meta.bio }}</p>
			<ul class="fh5co-social">
				{% if config.authorgithub %}
				<li><a href="{{ config.authorgithub }}" target="_blank"><i class="icon-github"></i></a></li>
				{% endif %}
				{% if config.authorlinkedin %}
				<li><a href="{{ config.authorlinkedin }}" target="_blank"><i class="icon-linkedin"></i></a></li>
				{% endif %}
				{% if config.authoremail %}
				<li><a href="{{ config.authoremail }}" target="_blank"><i class="icon-email"></i></a></li>
				{% endif %}
			</ul>
		</div>

		<div class="fh5co-menu">
			<div class="fh5co-box">
				<h3 class="heading">Categories</h3>
				{% if meta.purpose == "pico_categories_page" %}
				<ul>
					{% for cat, cated_pages in pico_categories_page %}
					<li><a href="categories">{{ cat }}</a></li>
					{% endfor %}
				</ul>
				{% endif %}
			</div>
			<div class="fh5co-box">
				<h3 class="heading">Search</h3>
				<form action="{{ 'search'|link }}" method="get">
					<div class="form-group">
						<input type="text" name="q" class="form-control" placeholder="Type a keyword">
					</div>
				</form>
			</div>
		</div>
	</div>
	
	<!-- END #fh5co-offcanvas -->
	<header id="fh5co-header" itemscope="itemscope" itemtype="http://schema.org/WPHeader">
		
		<div class="container-fluid">
			<div class="row">
				<a href="#" class="js-fh5co-nav-toggle fh5co-nav-toggle"><i></i></a>
				<ul class="fh5co-social">
					<li><a href="{{ config.authorgithub }}" target="_blank"><i class="icon-github"></i></a></li>
					<li><a href="{{ config.authorlinkedin }}" target="_blank"><i class="icon-linkedin"></i></a></li>
					<li><a href="{{ config.authoremail }}" target="_blank"><i class="icon-email"></i></a></li>
				</ul>
				<div class="col-lg-12 col-md-12 text-center">
					<h1 id="fh5co-logo" itemprop="name"><a href="{{ base_url }}" itemprop="url">{{ site_title }} </a></h1>
				</div>
			</div>
		</div>
	</header>
	<!-- END #fh5co-header -->
	
	<div class="container-fluid">
		<div class="row fh5co-post-entry hfeed" itemscope itemtype="http://schema.org/Blog">
		{% for page in pages|sort_by("time")|reverse %}
			{% if page.meta.template == "single" %}
			<article class="col-lg-3 col-md-4 col-sm-6 col-xs- 12 animate-box hentry">
				<figure>
					<a href="{{ page.url }}"><img src="{{ page.meta.thumbnail }}" alt="{{ page.title }}" class="img-responsive"></a>
				</figure>
				<span class="fh5co-meta"><a href="#">{{ page.meta.category }}</a></span>
				<h2 class="fh5co-article-title entry-title"><a href="{{ page.url }}">{{ page.title }}</a></h2>
				<span class="fh5co-meta fh5co-date">{{ page.date }}</span>
			</article>
			{% endif %}
		{% endfor %}
		</div>
	</div>

	<footer id="fh5co-footer" itemscope="itemscope" itemtype="http://schema.org/WPFooter">
		<p><small>&copy; {{config.copyright_year}}. {{ site_title }}. All Rights Reserverd. </small></p>
	</footer>

	<!-- jQuery -->
	<script src="{{ theme_url }}/js/jquery.min.js"></script>
	<!-- jQuery Easing -->
	<script src="{{ theme_url }}/js/jquery.easing.1.3.js"></script>
	<!-- Bootstrap -->
	<script defer src="{{ theme_url }}/js/bootstrap.min.js"></script>
	<!-- Waypoints -->
	<script defer src="{{ theme_url }}/js/jquery.waypoints.min.js"></script>
	<!-- Main JS -->
	<script src="{{ theme_url }}/js/main.js"></script>
	<script>
	$(document).ready(function(){
		$('.fh5co-post-entry').each(function() {
			var currentPage = 0;
			var numPerPage = {{ config.numPerPage }};
			var $table = $(this);
			$table.bind('repaginate', function() {
				$table.find('article').hide().slice(currentPage * numPerPage, (currentPage + 1) * numPerPage).show();
			});
			$table.trigger('repaginate');
			var numRows = $table.find('article').length;
			var numPages = Math.ceil(numRows / numPerPage);
			var $pager = $('<div class="pager"></div>');
			for (var page = 0; page < numPages; page++) {
				$('<span class="page-number"></span>').text(page + 1).bind('click', {
					newPage: page
				}, function(event) {
					currentPage = event.data['newPage'];
					$table.trigger('repaginate');
					$(this).addClass('active').siblings().removeClass('active');
				}).appendTo($pager).addClass('clickable');
			}
			$pager.insertAfter($table).find('span.page-number:first').addClass('active');
		});
	});
	</script>
	</body>
</html>